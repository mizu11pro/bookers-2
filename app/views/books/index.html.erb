<p><%= link_to "設定", edit_user_path(@user) %></p>

<h1>New book</h1>

<%= form_with model:@book, local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>opinion</h4>
<%= f.text_area :body %>

<%= f.submit 'create book'%>
<% end %>

<h1>Books</h1>
<h2>Title</h2>
<h2>Opinion</h2>
<% @books.each do |book| %>
<%= link_to user_path(@user) do %>
<%= attachment_image_tag @user, :profile_image, :fill, 40, 48, format: 'jpeg', fallback: "no_image.jpg" %>
 <% end %>
 <%= book.title %>
<%= book.body %>
<% end %>