<%= flash[:notice] %>

<%= render 'books/list', book: @book, user: current_user %>

<h1>Users</h1>
<h2>image</h2>
<h2>name</h2>

<% @users.each do |user| %>
<%= user.name %>
<%= link_to "show", user_path(user.id) %>
<% end %>